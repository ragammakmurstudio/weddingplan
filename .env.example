# Ganti dengan value acak yang panjang (mis. `openssl rand -base64 32`) di produksi
AUTH_SECRET="isi-secret-acak-anda"

# SQLite untuk dev. Untuk produksi ganti ke PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/nikahplan"
DATABASE_URL="file:./dev.db"
